const menu=document.querySelector('.menu');const links=document.querySelector('.links');
function setMenu(open){if(!menu||!links)return;links.classList.toggle('open',open);menu.setAttribute('aria-expanded',String(open));menu.setAttribute('aria-label',open?'Close menu':'Open menu');}
if(menu&&links){menu.addEventListener('click',()=>setMenu(!links.classList.contains('open')));document.addEventListener('keydown',e=>{if(e.key==='Escape')setMenu(false)});document.addEventListener('click',e=>{if(window.innerWidth<=900&&links.classList.contains('open')&&!links.contains(e.target)&&!menu.contains(e.target))setMenu(false)});}
document.querySelectorAll('.links a').forEach(a=>a.addEventListener('click',()=>setMenu(false)));
const year=document.querySelector('[data-year]');if(year)year.textContent=new Date().getFullYear();
function copyOffer(card){const text=card.getAttribute('data-copy-offer')||'';const done=()=>{card.classList.add('copied');const label=card.querySelector('.copy-label');if(label)label.textContent='Offer copied';setTimeout(()=>{card.classList.remove('copied');if(label)label.textContent='Click to copy offer';},1700)};if(navigator.clipboard&&window.isSecureContext){navigator.clipboard.writeText(text).then(done).catch(()=>fallbackCopy(text,done));}else{fallbackCopy(text,done)}}
function fallbackCopy(text,done){const ta=document.createElement('textarea');ta.value=text;ta.style.position='fixed';ta.style.opacity='0';document.body.appendChild(ta);ta.focus();ta.select();try{document.execCommand('copy');done()}catch(e){}document.body.removeChild(ta)}
document.querySelectorAll('.coupon[data-copy-offer]').forEach(card=>{card.addEventListener('click',()=>copyOffer(card));card.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();copyOffer(card)}})});